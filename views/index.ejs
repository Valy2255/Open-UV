<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Open UV</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <div class="container">
      <h1>Check UV Index</h1>
      <form action="/uv-data" method="post">
        <label for="latitude">Latitude:</label>
        <input
          type="text"
          id="latitude"
          name="lat"
          placeholder="Enter latitude"
          required
        />
        <label for="longitude">Longitude:</label>
        <input
          type="text"
          id="longitude"
          name="lng"
          placeholder="Enter longitude"
          required
        />
        <button type="submit">Get UV Index</button>
      </form>

      <% if(errorMessage) { %>
      <p class="error"><%= errorMessage %></p>
      <% } else if(uvData) { %>
      <div class="result"></div>
      <h2>UV Data:</h2>
      <p><strong>UV Index:</strong> <%= uvData.result.uv %></p>
      <p><strong>UV Max:</strong> <%= uvData.result.uv_max %></p>
      <% } %>
    </div>
  </body>
</html>
